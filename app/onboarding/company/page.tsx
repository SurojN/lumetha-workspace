import { createCompany } from "@/app/actions/company";
import { requireUser } from "@/lib/auth";
export default async function CompanyOnboarding() { const user = await requireUser(); return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><form action={createCompany} className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-8 shadow-sm"><h1 className="text-2xl font-semibold">Set up your company</h1><p className="mt-2 text-sm text-slate-500">You&apos;ll be the company admin. Invite and access rules can be managed from company settings.</p><label className="mt-6 block text-sm font-medium">Company name<input required name="name" className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" placeholder="Acme Inc." /></label><label className="mt-4 block text-sm font-medium">Allowed company email domain <span className="font-normal text-slate-400">(optional)</span><input name="emailDomain" className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" placeholder="acme.com" /></label><p className="mt-2 text-xs text-slate-500">When set, only members using this company domain may be added.</p><button className="mt-6 w-full rounded-lg bg-[#202b4b] px-4 py-2.5 font-medium text-white">Create workspace for {user.email}</button></form></main>; }
